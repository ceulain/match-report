import classes from "./styles.module.css";

const ActionHandler = () => {
  return (
    <div className={classes.wrapper}>
      <p>Action handler</p>
    </div>
  );
};

export default ActionHandler;
